# from fastapi import APIRouter

# from .endpoints import question_route, block_route

# main_router = APIRouter()

# main_router.include_router(question_route, prefix='/question', tags=['question'])
# main_router.include_router(block_route, prefix='/launch_parse', tags=['launch_parse'])
